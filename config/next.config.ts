// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove pageExtensions if you don't need them for .mdx pages
  // pageExtensions: ["js", "jsx", "ts", "tsx"], // default anyway
};

export default nextConfig;
